pluginManagement {
    repositories {
        google()
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = "kmp-hands-on-tutorial"
include(":androidApp")
include(":shared")